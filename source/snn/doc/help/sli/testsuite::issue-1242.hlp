Synopsis:

(issue-1242) run -&gt; NEST exits if test fails


Description:

Tests that a connection from a node without proxies to 
a node without proxies that has global targets throws an error 
(instead of failing silently or resulting in inconsistent results).


Author:

Jan Hahne


FirstVersion:

July 2019


Source:

/home/dansan/neuromorphic/source/snn/nest-simulator-2.20.0/testsuite/regressiontests/issue-1242.sli
